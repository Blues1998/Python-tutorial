# Build a calculator which supports the following operations: add, subtract, multiply,
# divide, floor divide, modulus, power
# The calculator should run indefinitely (hint: use infinite loop) until the user enters a special input.
# The choice of special input is up to you.
